{
  "name": "queryforge-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for QueryForge — holds the LLM key, builds prompts server-side.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
